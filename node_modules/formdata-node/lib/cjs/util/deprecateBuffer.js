"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("util");
function deprecateBuffer(_t, _k, descriptor) {
    let fn = descriptor.value;
    let decorated = false;
    descriptor.value = function (...args) {
        if (Buffer.isBuffer(args[1]) && !decorated) {
            decorated = true;
            fn = util_1.deprecate(fn, "The usage of Buffer in entry's value is deprecated. "
                + "Use Blob, File, fileFromPath() or fileFromPathSync() instead.");
        }
        return fn.apply(this, args);
    };
}
exports.default = deprecateBuffer;
